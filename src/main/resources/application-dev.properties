# POSTGRESQL DATABASE
spring.datasource.url=jdbc:postgresql://localhost:5432/vidara_trade_center
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / HIBERNATE
# ddl-auto options:
#   create      = drop & create tables every restart (DANGEROUS)
#   create-drop = drop tables when app stops
#   update      = update schema without losing data (GOOD for dev)
#   validate    = only validate, don't change schema
#   none        = do nothing

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# FILE UPLOAD DIRECTORY
file.upload-dir=uploads

# JWT SECRET
app.jwt.secret=YourSuperSecretKeyHereMakeItLongAtLeast256BitsForHS256Algorithm123456
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000

# LOGGING
logging.level.root=INFO
logging.level.com.vidara.tradecenter=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
# SMTP troubleshooting (disable after email works)
logging.level.org.springframework.mail=DEBUG
logging.level.org.eclipse.angus.mail=DEBUG

# POST /api/admin/mail/test (ADMIN JWT) — verify Gmail SMTP without placing an order
app.mail.test-endpoint-enabled=true


# PAYHERE PAYMENT GATEWAY (sandbox)
payhere.merchant-id=1234711
payhere.merchant-secret=MjU2MDkzNzkwMzE4MjM2MDg1MDIzNzE5NzkxNjExMzE0NDQwMzU4OQ==
payhere.sandbox=true
payhere.frontend-url=http://localhost:3000

# CORS ALLOWED ORIGINS (React frontend)
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173

# EMAIL / SMTP (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=vidaratradecenter@gmail.com

# App password: EXACTLY 16 characters, NO spaces (Gmail shows 4 groups — paste as one continuous string).
# Wrong length = authentication fails; you will see no mail and often no "Sent" copy in Gmail web UI for SMTP.
spring.mail.password=zubfkzppkugtluhq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# Must match the Gmail account you authenticate with, or a verified "Send mail as" alias
app.mail.from-address=vidaratradecenter@gmail.com
app.mail.from-name=Vidara Trade Center